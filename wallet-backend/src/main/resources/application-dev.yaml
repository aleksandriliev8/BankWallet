spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bank_wallet_db
    username: postgres
    password: Parolazasusi7777
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

logging:
  level:
    org.springframework.web: DEBUG